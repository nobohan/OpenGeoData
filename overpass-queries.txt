/* Restaurants in Wallonie */

[out:json][timeout:25];
{{geocodeArea:Wallonie}}->.searchArea;
(
  node["amenity"="restaurant"](area.searchArea);
  way["amenity"="restaurant"](area.searchArea);
);
out body;
>;
out skel qt;


/*
This has been generated by the overpass-turbo wizard.
The original search was:
“restaurant and cuisine=pizza in Wallonie”
*/
[out:json][timeout:25];
// fetch area “Wallonie” to search in
{{geocodeArea:Wallonie}}->.searchArea;
// gather results
(
  // query part for: “restaurant and cuisine=pizza”
  node["amenity"="restaurant"]["cuisine"="pizza"](area.searchArea);
  way["amenity"="restaurant"]["cuisine"="pizza"](area.searchArea);
  relation["amenity"="restaurant"]["cuisine"="pizza"](area.searchArea);
);
// print results
out body;
>;
out skel qt;

--> 78 seulement! regarder italian

/* regarder by regex */
[out:json][timeout:25];
// fetch area “Wallonie” to search in
{{geocodeArea:Wallonie}}->.searchArea;
// gather results
(
  // query part for: “restaurant and cuisine=pizza”
  node["amenity"="restaurant"]["name"~"pizz"](area.searchArea);
);
// print results
out body;
>;
out skel qt;


